console.log("Running front app");

//
  let circle = document.getElementById("firstCircle");

  let circleWidth = circle.offsetWidth;
  console.log(circleWidth);

circle.style.height =  circleWidth + "px";